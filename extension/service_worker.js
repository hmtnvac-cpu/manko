const STATE_KEY='manko_collector_state', API='https://manko-api.onrender.com';
async function getState(){const r=await chrome.storage.local.get(STATE_KEY);return r[STATE_KEY]||{queue:[],running:false,current:null,results:[],errors:[]}}
async function setState(s){await chrome.storage.local.set({[STATE_KEY]:s})}
function sourceFor(u=''){if(u.startsWith('https://manko.fun/movie-info/'))return'manko';if(u.startsWith('https://film4k.net/watch/'))return'film4k';return null}
async function postJson(path,body){try{const r=await fetch(API+path,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const t=await r.text();let d=null;try{d=JSON.parse(t)}catch(_){}return{ok:r.ok,status:r.status,data:d,text:t.slice(0,500)}}catch(e){return{ok:false,error:String(e?.message||e)}}}
async function processNext(){const s=await getState();if(s.running||!s.queue.length)return;const movieUrl=s.queue.shift(),source=sourceFor(movieUrl);if(!source){s.errors.push({movieUrl,error:'Unsupported URL'});await setState(s);return processNext()}s.running=true;s.current={source,movieUrl,movieTabId:null,playerTabId:null,title:null,poster:null,playerUrls:[],playerIndex:0,streams:[],metadata:{},candidates:[]};await setState(s);const tab=await chrome.tabs.create({url:movieUrl,active:false});s.current.movieTabId=tab.id;await setState(s)}
async function finishCurrent(ok,payload){const s=await getState(),cur=s.current||{},record={...cur,...payload,collectedAt:new Date().toISOString()};delete record.playerTabId;delete record.movieTabId;if(ok){if(cur.source==='manko')record.sync=await postJson('/collector/result',record);s.results=s.results.filter(x=>x.movieUrl!==record.movieUrl);s.results.push(record)}else{s.errors.push(record)}for(const id of [cur.movieTabId,cur.playerTabId])if(id)try{await chrome.tabs.remove(id)}catch(_){}s.running=false;s.current=null;await setState(s);processNext()}
async function openNextPlayer(){const s=await getState(),c=s.current;if(!c)return;if(c.playerTabId)try{await chrome.tabs.remove(c.playerTabId)}catch(_){};if(c.playerIndex>=c.playerUrls.length)return finishCurrent(c.streams.length>0,{streams:c.streams,streamUrl:c.streams[0]?.url||'',headers:c.streams[0]?.headers||{Referer:'https://javplayer.cc/'}});const u=c.playerUrls[c.playerIndex++];const tab=await chrome.tabs.create({url:u,active:false});c.playerTabId=tab.id;await setState(s)}
chrome.runtime.onMessage.addListener((msg,sender,sendResponse)=>{(async()=>{if(msg.type==='START_QUEUE'){const s=await getState();const incoming=[...new Set((msg.urls||[]).map(x=>x.trim()).filter(x=>sourceFor(x)))];const existing=new Set([...(s.queue||[]),s.current?.movieUrl].filter(Boolean));const added=incoming.filter(x=>!existing.has(x));s.queue.push(...added);await setState(s);processNext();sendResponse({ok:true,added:added.length});return}
if(msg.type==='BACKFILL_RESULTS'){const s=await getState();const urls=[...new Set((s.results||[]).filter(x=>x.source==='manko').map(x=>x.movieUrl).filter(Boolean))];s.queue=urls;s.running=false;s.current=null;await setState(s);processNext();sendResponse({ok:true,queued:urls.length});return}
if(msg.type==='MANKO_PLAYER_FOUND'){const s=await getState();if(!s.current)return;s.current.title=msg.title;s.current.poster=msg.poster||'';s.current.metadata=msg.metadata||{};s.current.playerUrls=[...new Set(msg.playerUrls||[msg.playerUrl].filter(Boolean))];s.current.playerIndex=0;s.current.streams=[];await setState(s);await openNextPlayer();sendResponse({ok:true});return}
if(msg.type==='JAVPLAYER_RESULT'){const s=await getState(),c=s.current;if(!c)return;if(msg.ok&&msg.streamUrl&&!c.streams.some(x=>x.url===msg.streamUrl))c.streams.push({name:'#'+(c.streams.length+1),url:msg.streamUrl,vttUrl:msg.vttUrl||null,headers:msg.headers||{Referer:'https://javplayer.cc/'},playerId:msg.playerId,playerUrl:msg.playerUrl});await setState(s);await openNextPlayer();sendResponse({ok:true});return}
if(msg.type==='SYNC_CATALOG'){sendResponse(await postJson('/collector/catalog',{pageUrl:msg.pageUrl,urls:msg.urls||[]}));return}if(msg.type==='GET_STATE'){sendResponse(await getState());return}if(msg.type==='CLEAR_RESULTS'){const s=await getState();s.results=[];s.errors=[];await setState(s);sendResponse({ok:true});return}})();return true});
chrome.runtime.onInstalled.addListener(()=>processNext());chrome.runtime.onStartup.addListener(()=>processNext());
